version: 2.1

orbs:
  maven: circleci/maven@0.0.12

workflows:
  maven_test:
    jobs:
      build:
        docker:
          - image: cimg/openjdk:15.0.1
        steps:
          - checkout
          - run: mvn clean
          - run: mvn package
